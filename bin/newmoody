#!/usr/bin/env bash
set -euo pipefail
BIN="${JOURNAL_BIN:-${KLR_BIN:-$HOME/bin}}"
# Legacy name — create moody for a date (default today)
thedate="${1:-$(date +%F)}"
exec "$BIN/journal_create_moody" -d "$thedate" -f

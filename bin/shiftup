#!/bin/bash
last=$(ls -1 | sort | tail -n 1 | sed -e 's/[^[:digit:]]//g' -e 's/^0*//')
first=$1
for ((i=last;i>=first;i--))
do
	j=$(($i+1))
	mv IMG_$(printf '%04i' $i).JPG IMG_$(printf '%04i' $j).JPG
done
exit
